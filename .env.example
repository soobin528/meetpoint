# MeetPoint 환경 변수 예시 파일
# 실제 배포/개발 환경에서는 .env 파일을 생성해서 사용하고
# 이 파일(.env.example)은 템플릿 용도로만 사용합니다.

########################################
# PostgreSQL / PostGIS 설정
########################################

# 데이터베이스 이름
POSTGRES_DB=meetpoint

# 데이터베이스 사용자
POSTGRES_USER=meetpoint

# 데이터베이스 비밀번호
POSTGRES_PASSWORD=meetpoint

# 데이터베이스 호스트
# - docker-compose 사용 시: 서비스 이름인 "db" 를 사용
# - 로컬 직접 실행 시: localhost 또는 실제 호스트명
POSTGRES_HOST=db

# 데이터베이스 포트
POSTGRES_PORT=5432

# SQLAlchemy 에서 사용할 전체 데이터베이스 URL
# - psycopg2 드라이버 사용
DATABASE_URL=postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}


########################################
# Redis 설정
########################################

# Redis 연결 URL
# - docker-compose 사용 시: redis 서비스 이름 사용
REDIS_URL=redis://redis:6379/0


########################################
# Kakao Local API (POI 추천)
########################################

# Kakao 개발자 콘솔에서 발급 (필수)
KAKAO_REST_API_KEY=your_kakao_rest_api_key_here
KAKAO_LOCAL_BASE_URL=https://dapi.kakao.com

# POI 검색 반경(m), 캐시 TTL(초), 최소 갱신 간격(초), 최소 이동 거리(m)
POI_RADIUS_M=1000
POI_CACHE_TTL_SEC=120
POI_MIN_REFRESH_SEC=3
POI_MIN_MOVE_M=50


########################################
# 애플리케이션 공통 설정 (필요 시 확장)
########################################

# FastAPI 환경 (예: development, production)
APP_ENV=development

