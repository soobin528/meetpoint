# MeetPoint 프로젝트 Python 의존성 목록
# 각 패키지 버전은 2024년 기준 안정 버전으로 지정

fastapi==0.115.0          # FastAPI 웹 프레임워크
uvicorn[standard]==0.30.0 # ASGI 서버 (개발용)

SQLAlchemy==2.0.25        # ORM 및 DB 추상화
psycopg2-binary==2.9.9    # PostgreSQL 드라이버
geoalchemy2==0.15.2       # PostGIS 지원 SQLAlchemy 확장

redis==5.0.1              # Redis 클라이언트
pydantic==2.7.0           # 데이터 검증 및 설정 모델
python-dotenv==1.0.1      # .env 환경 변수 로딩

